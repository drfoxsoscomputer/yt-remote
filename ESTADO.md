# ESTADO — YT-Remote

> Proyecto de control rem YouTube con ventana nativa y interfaz web-based.

## Qué hace
Launcher multiplataforma que muestra un formulario HTML servido por Flask (puerto 8081) en una ventana nativa pywebview + WebView2. El usuario configura token del bot, ID de admin y horas kick, y la app minimiza al sistema tray cuando el bot está conectado.

## Cómo correr

```powershell
# Modo desarrollo (Flask + HTML directo):
python -m src.launcher_web

# Compilado a .exe (onedir, sin splash):
dist\ytremote\ytremote.exe
```

## Estructura
- `src/main_launcher.py` — Entry point: mutex instancia única, check WebView2, hilo Flask daemon, `webview.create_window` (420x640, centrada en el área de trabajo), `LauncherApi`, events.closing → _on_closing, `webview.start()`, `_apagar_todo` en finally
- `src/launcher_web.py` — Flask API routes: `/launcher` (GET), `/api/session` (GET), `/api/connect` (POST), `/api/status` (GET), `/shutdown` (POST)
- `templates/launcher.html` — Formulario 420×520: token, admin ID, horas kick, botones en fila (Conectar azul / Salir fantasma / Detener / Cerrar sesión rojo); logo `static/img/logo-ytremote.png`; Tailwind + JS `pywebview.api`
- `static/css/tailwind.css` + `tema.css` — Paleta Telegram (#229ed9 azul, #31c471 verde, #e53935 rojo; sin dorado); recompilar con `npx tailwindcss -i static/css/input.css -o static/css/tailwind.css --minify`
- `static/js/launcher.js` — Form submit, toggle eye, `pywebview.api` calls, refleja `/api/status`, error handling
- `static/img/logo-ytremote.png` — Copia del logo (servida por Flask; entra al bundle vía `static`)
- `src/bot_process.py` — BotProcess class (start/stop/is_running), reutilizable
- `build.py` — PyInstaller onedir build; `--add-data` runtimes WebView2 desde `assets/webview2/runtimes` (vendeados, sin rutas de la máquina); `--manifest` dpiAware; `--icon` ytremote.ico; `--splash` QUITADO. El ZIP (`--zip`) lo delega en `release_zip.py`
- `release_zip.py` — Módulo compartido que arma el ZIP portable para ambos builders; consume las reglas de `release_rules.json` (única fuente: versión, allowlist de site-packages, `include_top_files`, `folders`); modo onedir (exe + `_internal`) o onefile (`exe_file`)
- `release_rules.json` — Versión 1.0.0; allowlist de runtime recortada a lo que el bot importa de verdad (telegram+httpx+apscheduler, etc.); sin basura de desarrollo
- `dist/ytremote/ytremote.exe` — Ejecutable (18.0 MB, onedir, sin splash)
- `ytremote.manifest` — `dpiAware=true` + Microsoft Windows Common Controls 6.0 (evita re-escala ~2.5s)
- `assets/logo-ytremote.png` — Logo oficial (fuente para `static/img/logo-ytremote.png` y branding)
- `assets/webview2/runtimes/` — WebView2Loader.dll vendeados (win-x64/x86/arm64) para el bundle

## Estado actual
**RELEASE ZIP v1.1.2 PUBLICADO (14/sep/2026, 92.0 MB)** — `yt-remote-1.1.2-portable.zip` (3479 entradas): bump de parche por fix VISUAL del avatar-dot (v1.1.1→1.1.2). `release_rules.json version: "1.1.2"` → `python build.py --zip` → exe 18.1 MB + zip 92.0 MB. Cambio desde v1.1.1: el badge de estado del avatar quedaba separado del círculo; ahora queda "mitad adentro / mitad afuera" sobre el borde inferior derecho (`-bottom-0.5 -right-0.5` = 2 px, Opción A) aplicado en `templates/launcher.html:52` y `static/js/launcher.js:234` (antes `-bottom-1 -right-1` de 4 px que dejaba el dot tangente al círculo visible del avatar `w-8 h-8` rounded-full; también hubo un `-bottom-1.5` intermedio que se veía demasiado lejos). GOTCHA resuelto en el camino: `tailwind.css` compilado estaba STALE (contenía `-bottom-3`/`-right-3` de un build viejo y NO las clases del fuente) → recompilar SIEMPRE con `npx tailwindcss -i static/css/input.css -o static/css/tailwind.css --minify` tras cambiar clases. Verificado dentro del zip: dot `-bottom-0.5` presente en `_internal/templates/launcher.html`, `_internal/static/js/launcher.js` y `._internal/static/css/tailwind.css` (`.-bottom-0\.5{bottom:-.125rem}`); `data/` SOLO con `.gitkeep` (sin `session.enc`/`roles.db`/`bot.log` = no filtra datos); MD5 fuente↔empaquetado (normalización LF): `launcher.js` 6b82ede…, `launcher.html` c376896…, `tailwind.css` 2179049… todos OK en dist y zip. `dist\ytremote` materializado portable + `data/` real restaurada del snapshot `ytremote-data-snap-20260914c` (3 MD5 idénticos: session.enc 1DF710C3, bot.log ABAF3449, roles.db BEA63939). El helper `extraer_portable.ps1` (temp) apunta al zip 1.1.2. Release: https://github.com/drfoxsoscomputer/yt-remote/releases/tag/v1.1.2 (latest=true, nota de portabilidad WebView2 honesta). Commits pusheados: `94433a5` (fix badge) y `9fc54f4` (release 1.1.2). Checklist 7/7 cerrado.

**Avatar-dot pegado al avatar + CONEXIÓN RESTAURADA + rutina post-build documentada (14/sep/2026, SIN release)**: el usuario reportó que el círculo del badge del avatar quedaba demasiado lejos (`-bottom-3 -right-3` = 12 px) y que el exe nueva vez "no conectaba" (ventana con error). Dos causas separadas: (1) **avatar-dot** → `-bottom-1 -right-1` (4 px) aplicado en `templates/launcher.html:52` y `static/js/launcher.js:234` (pintarEstado re-aplica la posición). (2) **Conexión**: el log `data/bot.log` decía `No se encontro ...\dist\ytremote\runtime\python\python.exe. Ejecuta la app desde la carpeta completa (runtime junto al .exe)`. Causa raíz: `python build.py` regenera `dist\ytremote` con PyInstaller `--clean` y **borra TODO lo que está junto al exe** (`runtime/`, `src/`, `config.json` y `data/`); en el rebuild del fix de colores se restauró `data/` pero se omitió re-ejecutar `extraer_portable.ps1`. **RUTINA POST-BUILD obligatoria**: tras cada `python build.py` → 1) `C:\Users\DrFox\AppData\Local\Temp\opencode\extraer_portable.ps1` (restaura `runtime/`+`src/`+`config.json` desde el zip 1.1.0; nunca toca exe/_internal), 2) restaurar `data/`. **GOTCHA de restauración (causó has_session:false)**: `Copy-Item -Force "$snap\*" dist\ytremote\data` con el destino `data\` INEXISTENTE (recién borrada por el build) crea un archivo basura de 390 B llamado `data` en vez de volcar los 3 archivos → `api/session` da `has_session:false` porque `session.enc` no existe. Restauración correcta: `New-Item -ItemType Directory` primero y luego `Copy-Item` archivo por archivo (o a una `data\` que YA exista). Smoke final exe real (CWD=dist\ytremote, PID 10064): `/launcher` 200, `/api/session` `has_session:true` (admin real, kick 0), `/api/start` 200 `"Bot conectado (@ytremoto_bot)"`, `/api/status` `bot_running:true`, JS servido con `-bottom-1 -right-1`; MD5 post-fix: `launcher.js` **E4B42CBF...** y `launcher.html` **DBA02988...** idénticos árbol↔`_internal`; `tailwind.css`/`tema.css` **sin cambios** (7935BB08..., 3C113CA6...). `data/` restaurada a snapshot `ytremote-data-snap-20260914c` (session.enc 1DF710C3..., bot.log 14567, roles.db 20480 — los 3 MD5 idénticos al snapshot), procesos 0, socket 8081 cerrado. Detalle menor anotado: `launcher_web.py:38` define `SESSION_FILE = BASE_DIR / "session.enc"` que está MUERTO (los helpers usan `SessionManager` → `data/session.enc`); candidato a limpieza.

**Colores de estado rotos por orden en `setClases()` — CAUSA RAÍZ + fix (14/sep/2026, SIN release)**: el usuario reportó que, independientemente del estado, TODO se veía blanco/gris, el SVG del Power solo cambiaba en hover (verde), y el badge/dot/avatar no se pintaban. Causa raíz: `setClases()` (`static/js/launcher.js:37-43`) hacía `classList.add(...)` ANTES que `classList.remove(...)`, y las remove-lists (`CLS_CHIP`, `CLS_DOT`, power, icono) son el vocabulario COMPLETO de colores — coinciden con los tokens que el estado recién añade, así que el remove borraba exactamente lo pintado. Simulación node sobre clases reales: chip/dot/avatar/icono quedaban (VACÍO) en los 5 estados; solo sobrevivían tokens ajenos a la remove-list (`group-hover:text-verde-hover`/`text-rojo-hover`) → por eso el base siempre blanco/gris y el hover verde/rojo. Bug de larga data (desde el rediseño commit `99e1a31`), NO introducido por los fixes 1-7. **Fix (1 línea)**: en `setClases()` el `remove` corre ANTES del `add` (semántica "reemplaza el estado anterior"). Exe reconstruido (`python build.py`, 18.1 MB): MD5 bundle↔árbol de `launcher.js` idéntico (146B423528B8DD08740AC9DC1B70A503), bundle con orden remove→add (l.41-42); smoke exe real (PID 3444): `/launcher` 200, `/static/js/launcher.js` servido MD5 idéntico y con orden remove→add, `/api/session` 200 `has_session:true` + `Cache-Control: no-store`; `data/` restaurada del snapshot (session.enc MD5 CC2537... idéntico), procesos 0, socket 8081 cerrado. Pendiente: solo E2E visual del usuario.

**Exe reconstruido con TODOS los fixes 1-7 y smoke-probado (14/sep/2026, SIN release)**: `python build.py` a secas → `dist\ytremote\ytremote.exe` 18.1 MB (el 1er build falló con `WinError 5 Acceso denegado` en `runtime\python\libcrypto-3.dll`: el bot del smoke previo seguía vivo reteniendo el DLL — matar el python de runtime antes de buildear); portabilidad restaurada (2243) y `data/` respaldado/restaurado (snapshot `ytremote-data-snap-20260914b`). `_internal` verificado: `w-20`/`h-20` ✓, guarda `sin minimizar` ✓, `location.reload` único y en la rama else (logout sin doble navegación) ✓, `tema.css` sin `form-container` ✓, MD5 launcher.js/tailwind.css/tema.css idénticos árbol↔`_internal`. Smoke del exe REAL (CWD=dist\ytremote, PID 7580): 200 ×5 (`/launcher`, `/api/session`, `/api/status`, `/static/js/launcher.js`, `/static/css/tailwind.css`); `/api/session` con `Cache-Control: no-store`; POST save con `Origin: http://evil.example` → 403 `Origen no permitido`; POST legit (Origin propio) con `kick_after_hours:999` → 200 y sesión guardada con `kick=168`; POST sin Origin → 200 (cliente no-navegador legítimo). El smoke sobreescribió la sesión con una fake (9:AAA) → `data/session.enc` DIST restaurada del snapshot (MD5 idéntico), procesos 0, socket 8081 TimeWait limpio. ZIP 1.1.0 intacto, versión sin tocar.

**Auditoría web 14/sep COMPLETA: fixes 3-7 aplicados y verificados en dev (SIN release)**: (3) voseo `launcher_web.py` "Iniciá..." → "Inicie sesión primero."; (4) logout sin doble navegación (launcher.js: el `location.reload()` solo ocurre en el fallback sin pywebview; con pywebview `logout()` nativo ya navega a /launcher); (5) `#form-container` muerto removido de `tema.css`; (6) **anti-CSRF local** en launcher_web.py (`@app.before_request`: en POST/PUT/PATCH/DELETE, si hay header `Origin` debe ser `http://<Host>` → fetch de una página externa recibe 403, un cliente no-navegador pasa sin Origin) + `Cache-Control: no-store` en `/api/session` (datos sensibles en claro en la respuesta); (7) **tope 168 en backend**: `KICK_MAX_HOURS=168` + `_clamp_kick_hours()` en `/api/save` y `/api/connect`, clamp en `LauncherApi.connect` (main_launcher.py) y guarda final en `config.py` (`min(168.0, max(0.0, ...))`, aunque venga por env/config.json). Verificado en dev (server `python -m src.launcher_web`): `/api/session` 200 + `Cache-Control: no-store`; POST `/api/start` sin sesión → 400 `"Inicie sesión primero."`; POST con `Origin: http://evil.example` → 403 `Origen no permitido`; POST sin Origin → 200 (cliente legítimo no-navegador); POST con `Origin: http://127.0.0.1:8081` → 200; save con `kick_after_hours:999` → sesión guardada con `kick_after_hours:168`. `py_compile` (launcher_web, main_launcher, config) y `node --check` OK. `data/session.enc` dev respaldado/restaurado tras la prueba.

**Exe reconstruido y smoke-probado con los fixes 1-2 (14/sep/2026, SIN release)**: `python build.py` a secas (exe `dist\ytremote\ytremote.exe` 18.1 MB, sin zip); portabilidad restaurada del zip 1.1.0 (2243 archivos: `runtime/`, `src/`, `config.json`, docs — sin tocar exe/_internal) y `data/` respaldado/restaurado (snapshot `ytremote-data-snap-20260914` → bot.log 2086, roles.db 20480, session.enc 358). Verificado: `_internal` con `w-20`/`h-20` (1 ocurrencia c/u) y JS con guarda `sin minimizar` l.520; hashes MD5 `launcher.js` y `tailwind.css` IDÉNTICOS entre árbol y `_internal` (66D2..., 7935...); smoke real del exe (PID 7220, CWD=repo, puerto 8081 libre) → 200 en `/launcher`, `/api/session`, `/api/status`, `/static/js/launcher.js`, `/static/css/tailwind.css`; contenido SERVIDO con guarda ENCENDIDO + clases `.w-20`/`.h-20`; socket 8081 quedó en TimeWait (sin procesos residuales). ZIP 1.1.0 intacto, versión sin tocar.

**2 fixes de la auditoría parte web (14/sep/2026, SIN release)**: (1) `tailwind.css` recompilado (`npx tailwindcss -i static/css/input.css -o static/css/tailwind.css --minify`, 708 ms) → `w-20`/`h-20` de `#power-icon` (`templates/launcher.html:88`) ya existen en el CSS servido (verificado en dev y en el smoke 200); el SVG del Power vuelve a tener tamaño explícito 80×80 px (la ESTADO vieja decía 128 px=w-32, que nunca existió al compilarse; el documento queda corregido acá). (2) `launcher.js conectar()` ya minimiza al tray SOLO si `estadoUI === E.ENCENDIDO`: si el bot murió justo tras `/api/start`, la ventana queda visible mostrando Desconectado y el log marca `conectar: bot no quedo Encendido, sin minimizar`. Verificado: `node --check` OK, smoke 200 en 6 endpoints, CSS servido con `w-20`/`h-20`, JS servido con la guarda ENCENDIDO (sin regresión en boot: el arranque con sesión sigue minimizando en verde vía `cargarEstado(true)`).

## Estado actual (previo)
**Icono Power = el SVG del usuario, un solo icono en TODOS los estados (13/sep/2026, 5 pasos, SIN release)**: el usuario pegó `assets/power-button.svg` porque el icono del botón a veces "deja o muestra otro icono feo" que no parecía power on/off (era el `ICONO_SPINNER` que rotaba durante CONECTANDO). Ahora el botón usa SIEMPRE la forma exacta de su SVG **nueva** (viewBox 800, 2 rutas visibles clase `.st0` — aro abierto + vástago; el disco blanco y el aro exterior tienen `display:none` en el SVG original) teñida por estado vía `fill="currentColor"`: verde En línea, rojo Desconectado/Error, gris Sin sesión, verde en Conectando. **Tamaño del icono doblado a 128×128 px** (`w-32 h-32` en `templates/launcher.html:88`), botón a 144px (`w-36 h-36` sin tocar). Cambios: (1) `templates/launcher.html` `#power-icon` → viewBox 800, `fill="currentColor"`, solo 2 rutas; (2) `launcher.js` `ICONO_POWER` = las 2 rutas del SVG, eliminados `ICONO_SPINNER` y `animate-spin` (CONECTANDO usa el mismo power); (3) `static/img/power-button.svg` copiado y empaquetado. Verificado: `node --check` OK, build OK (18.1 MB), data restaurada del snapshot (hash session.enc idéntico, 358 B), portabilidad OK, `_internal` idéntico al árbol, servidos sin `ICONO_SPINNER`/`M21 12a9`/`animate-spin`, sin `fill="#ffffff"`, `power-button.svg` 200 y empaquetado; probe en vivo ciclo completo: boot → En línea → 3 s → minimizó al tray, procesos 0. **Pendiente: E2E visual del usuario.**

**CAUSA RAÍZ REAL del launcher CONGELADO en "Error"/"Conectando" (13/sep/2026 ~01:10 → 01:35)**: el JS SÍ cargaba y SÍ llamaba `/api/session` y `/api/status` (telemetría en vivo: `cargarEstado(primerPaso=S)`, `session has_session=true`, `status bot_running=true`), pero `pintarEstado()` lanzaba `TypeError: lista.flatMap is not a function` en `tokens()` (`launcher.js` ~23–25). **`tokens(lista)` recibía STRINGS** (c.badge, BADGE_BASE) y llamaba `lista.flatMap(...)` — pero `String.prototype.flatMap` no existe en NINGÚN motor JS, así que `pintarEstado()` reventaba al primer `setClases()`: se pintaba el badge-text ("Error"/"Conectando") y detalle/icono/accion/footer quedaban estáticos para siempre. Eso explica TODOS los síntomas del usuario (badge "Error", detalle "Aún no hay sesión…", sin botón, "puso Conectando y no hizo más nada"). **Fix en 7 tareas (13/sep 01:10–01:35, SIN release)**: (1) `tokens()` reescrito ES5-safe (`Array.isArray` + loop join/split/filter, sin flatMap); (2) telemetría end-to-end: `POST /api/log` en launcher_web.py + helpers `uiLog()` y `req()` con AbortController/timeout en launcher.js + `armaError` loguea causa real + captores globales `error`/`unhandledrejection` → `data/bot.log`; (3) `TRAY_TRAS_EN_LINEA = 3000` + `esperarYMinimizar()` = spec exacta del usuario (visible mientras conecta; tras En línea espera 3 s y se va sola al tray); (4) HTML estático arranca "Conectando..." (badge verde + spinner). **Verificación QUE PRUEBA EL CICLO COMPLETO** (exe 19 021 259 B, CWD=dist\ytremote): boot → `cargarEstado(primerPaso=S)` → session ok → bot_running → estado encendido → "En línea → espera 3000ms" → "minimizó al tray" → 0 procesos reales residuales, `data/` intacta (session.enc 358 B conservada). La causa CWD de las 01:00 sigue resuelta (`send_from_directory(app.static_folder or RES_DIR / "static", filename)` → absoluta `_internal/static` + glyph Power). **Pendiente: solo E2E visual del usuario con el exe.**

---

## Estado actual (historia — antes de la causa flatMap)
**CAUSA RAÍZ REAL del "DICE LO MISMO" (13/sep/2026 ~01:00)**: el exe SÍ leía la sesión y SÍ corría el bot (log 00:44–00:46 con token real, `session.enc` re-saved), pero la ventana seguía mostrando "Sin sesión" + reloj. El bug no era el JS: `src/launcher_web.py` tenía la ruta `/static/<path:filename>` con `send_from_directory("static", filename)` **relativa al CWD**. Cuando el usuario abre `dist\ytremote\ytremote.exe` el CWD es `dist\ytremote\` (donde NO hay `static\`, los assets viven en `_internal\static\`) → `/static/js/launcher.js`, `tailwind.css`, `tema.css` y el logo devuelven **404** → el JS jamás carga → la ventana queda en el HTML estático: reloj + "Sin sesión". Mi probe anterior "pasaba" por casualidad: corría el exe con el CWD en la raíz del repo (que sí tiene `static/`). **Fix (2 cambios, 5 tareas verificadas, SIN release)**: (1) `send_from_directory(app.static_folder or RES_DIR / "static", filename)` → siempre la carpeta absoluta `_internal/static`; (2) glyph estático `#power-icon` en `templates/launcher.html` reemplazado por el Power real con corte (`M18.36 6.64a9 9 0 1 1-12.73 0` + `M12 2v10`, stroke 2.5) → ya no hay reloj ni en el primer frame. **Verificación QUE REPRODUCE el escenario real**: exe lanzado con `WorkingDirectory=dist\ytremote` → TODOS los estáticos 200 (JS 21840 B idéntico al árbol, CSS×2, logo, skeleton), `/launcher` 200 con glyph Power y sin reloj, `/api/session has_session:true`, `/api/status bot_running:true`, procesos 0 tras matar, `data/` intacta. Sesión actual `session.enc` (358 B) guardada por el propio usuario a las 00:46:54 con `bot_username` vacío (cosmético, la UI sigue leyendo `bot_running`). Pendiente: solo E2E visual del usuario. el paso 5 del lote anterior (quitar el link de Telegram) se hizo con el shell (`Set-Content -NoNewline` sobre un array) → `static/js/launcher.js` quedó colapsado a UNA sola línea y el `//` del primer comentario se tragó TODO el archivo. Resultado idéntico a lo que vio el usuario: el JS parseaba (`node --check` pasa sobre un archivo todo-comentado) pero NINGÚN listener se registraba ni `window.__refresh_estado` se definía → estado "Sin sesión" estático, ícono de reloj (el HTML estático nunca se repintaba con el símbolo de Power real) y clics muertos. Diagnóstico duro: harness Node DOM-stub → antes `refresh_estado=undefined, listeners=[]`; después del fix `refresh_estado=function` + 5 listeners doc/window + 9 de elementos. El usuario además había borrado `data/` a mano (al ver el exe muerto), pero el exe NO se auto-limpia: con `data/session.enc` restaurada, `/api/session` → `has_session:true` real. **Fix**: archivo restaurado desde el zip 1.1.0 (multi-línea, la versión que SÍ funcionó en producción) y se reaplicaron los 5 cambios del lote vía editor de archivos (nunca más shell para escribir fuentes): `MIN_CONEXION=3000`, CONECTANDO verde, `cargarEstado(primerPaso)` → verde mínimo 3s → tray si `bot_running`, remove-lists `CLS_CHIP`/`CLS_DOT`, sin link. Build de prueba (`python build.py` a secas) → exe 18.1 MB; exe real: `/api/session` `has_session:true` + `bot_running:true` (arrancó el bot con la sesión), JS servido 21148 B / 582 líneas (idéntico al árbol), CSS con `poderNeon`, HTML sin link, `data/` intacta tras correr+matar. **Pendiente: E2E visual del usuario.**

**Arranque con animación verde + colores reales + sin link t.me (13/sep/2026, 5 tareas verificadas, ¡sin release!)**: (1) Python `_cargar_interfaz_real()` ya NO minimiza sola la ventana al arrancar con sesión: arranca el bot y se queda visible para la animación (docstring actualizado). (2) CSS `.power-connecting` pasó de azul a **verde neón pulsante** (`@keyframes poderNeon`, rgba(34,197,94,0.35→0.75)); `poderLatido` (azul) eliminado. (3) JS `cargarEstado(primerPaso)`: con datos guardados SIEMPRE pinta "Conectando…" verde neón, espera el `/api/status` (hasta 6s) y garantiza mínimo `MIN_CONEXION = 3000` ms visibles; recién cuando queda `bot_running` true hace `pywebview.api.minimizar_tray()`; si el bot no enciende, se queda visible en su estado real (Desconectado), sin minimizar. `configEstado(CONECTANDO)` todo verde (chip, circulito, avatar, spinner). (4) **Causa raíz del desmadre de colores**: `setClases()` recibía la lista "quitar" SOLO en el botón Power; el chip `#estado-badge`, el `#badge-dot` y el `#avatar-dot` se pintaban sin remover → las clases viejas se apilaban (`bg-verde` + `bg-rojo` + `bg-texto-dim`…, ganaba el orden del CSS) → "DESCONECTADO" y "EN LÍNEA" con círculo rojo + texto rojo + fondo verde juntos, y avatar rojo conectado. Fix: constantes `CLS_CHIP` y `CLS_DOT` (todas las clases de color posibles por elemento) pasadas como lista "quitar" en `pintarEstado()` → cada estado pinta TODO de un color verdadero, sin mezclas. (5) Botón **"Abrir en Telegram" ELIMINADO** (HTML `<a id="link-telegram">` + JS const `linkTelegram` + bloque `c.link` de `pintarEstado` + campo muerto `link:` en `configEstado` — usuario: "no le veo uso"). Verificado (dev, SIN release): py_compile OK, node --check OK, clases de opacidad presentes en tailwind.css (`.bg-verde/10`, `.bg-rojo/10`, `.border-verde/25`, `.border-rojo/30`, `.text-verde`, `.text-rojo` — primer grep falló por escape del backslash, no por Tailwind), smoke dev 200 en /launcher sin `link-telegram`/"Abrir en Telegram", chip base `bg-texto-dim/10` ok, /api/session `{"has_session":false}`. **ZIP/versión NO tocados**: el rebuild queda SOLO para el release cuando el usuario lo pida (regla acordada 13/sep: no duplicar trabajo ni mover la versión en cada cambio).

**Polish final del launcher + español neutro (12/sep/2026, 5 tareas verificadas)**: (1) La X de la ventana minimiza DIRECTAMENTE al tray sin diálogo — eliminado el MessageBoxW de `_on_closing()` (antes preguntaba "¿Minimizar al lado del reloj? (El bot seguirá corriendo)"); salir del todo = menú del tray "Salir" o Cerrar sesión (cierre programático). (2) Botón ✕ interno del header (`#btn-min`) ELIMINADO (HTML+JS): minimizar queda solo con la X de la ventana. (3) Atrás arreglado de raíz: `cargarEstado()` llama `mostrarVista('estado')` (también arregla el retorno a Estado tras Guardar/Conectar). (4) Botón Guardar sin emoji: SVG floppy (feather save, `currentColor`) + texto, `restaurarBotonGuardar()` restaura con `innerHTML`. (5) TODA la UI en español neutro Venezuela (usted, sin voseo): 11 strings en launcher.html (Pulsa→"Haga clic en", Creá tu→"Cree su", Averigüá tu→"Averigüe su", Inicia→"Inicie", Tu ID→"Su ID"), 10 en launcher.js (Haz/Pulsa/Toca→"Haga clic", Revisá/volvé→"Revise...e intente", Necesitás→"Necesita", Ingresa→"Ingrese"), launcher_web.py (2× "Revise el token y el ID"), main_launcher.py ("Revise el token...", "Instalalo"→"Instálelo", "Buscá"→"Busque"), legacy launcher.py (5 strings), bot.py (3 strings reales: `necesitas`→`necesita el rol`, "a tu chat privado"→"a su chat privado" ×2) y setup_cli.py ("Pulsa Enter"→"Presione Enter"). Verificado: node --check + py_compile OK; barrido de voseo = 0 en src/ y templates/ (los únicos "pulsa" residuales son subcadenas de "expulsar/se expulsa"); tailwind recompilado; smoke dev 200 en /launcher, /api/session `{"has_session":false}`, /js, /css, sin 💾 en JS servido; zip 1.1.0 rebuild (92,0 MB) + dist materializado (exe 18.1 MB, runtime presente). Pendiente: E2E del usuario.

**Fix raíz del paint + UI según feedback (12/sep/2026, 3 tareas verificadas)**: la causa de TODOS los síntomas anteriores ("ícono no cambia", "chip no se estiliza", modal genérico "Ups, algo salió mal / Detalle técnico") era UN bug: `setClases()` pasaba cadenas con espacios (`'inline-flex items-center gap-2...'`) como UN solo token a `classList.add()` → Chromium lanza `InvalidCharacterError` ("Failed to execute 'add' on 'DOMTokenList'") en CADA pintado, y la excepción escapaba a la red de seguridad global que mostraba el modal genérico. Fix: `setClases()` parte cada entrada con `split(/\s+/)` y descarta vacíos (helper `tokens()`). Además: (2) ícono central = símbolo universal de Power ⏻ (aro + línea, stroke 2.5); OFF/Desconectado rojo (borde + glow + ícono, `power-idle`) ya pintaba al aplicarse las clases. (3) Modal `#app-error` ELIMINADO del código (HTML+JS); `onerror`/`unhandledrejection` → `console.error` silencioso (el motivo va a data/bot.log), JAMÁS a la UI; errores reales se cuentan en el detalle del estado Error con una acción debajo del botón: token rechazado/sesión → "Ir a Configuración" (abre el form), red caída/bot no arranca → "Reintentar". Sin sesión → estado limpio "Sin Sesión" con botón verde "Iniciar sesión" → abre Configuración con el banner. `conectar()` primero lee /api/session: sin credenciales → form, nunca Error. Verificado: node --check OK, tailwind recompilado, smoke dev (no queda rastro de app-error/Entendido/Detalle técnico/btn-fantasma; session fake BORRADA → `{"has_session":false}`), zip 1.1.0 rebuild (js 20990B, html 9672B, tailwind 14769B). Pendiente: E2E del usuario (arranque sin sesión → Iniciar sesión; token real; token inválido → Error + Ir a Configuración; ⏻ rojo en Desconectado).

## Estado actual (previo: launcher de dos pantallas)
**Launcher de dos pantallas aprobado y commiteado (`637da6b`, 12/sep/2026 noche)**: PUEDES conectarte/ver estado en la ventana principal y editar datos en el formulario. Vista Estado: Sin sesión (botón "Iniciar sesión") / Conectando (#229ed9) / Conectado (#31c471, con Detener) / Detenido (#a1a1aa, con Conectar) / Error (#e53935 con motivo). Vista Formulario: Guardar SOLO guarda (no arranca, valida token no vacío + ID numérico); Conectar arranca con la sesión guardada y minimiza al tray; Detener se queda en la vista de estado, NO navega ni borra; Cerrar sesión es lo único que borra (con confirmación); Configurar precarga los datos. API: `GET /api/session` devuelve datos completos, `POST /api/save` (nuevo), `POST /api/start` (nuevo, getMe antes de arrancar → "Token inválido: ..." honesto). `LauncherApi.minimizar_tray()` y Detener desde tray vía `evaluate_js(__refresh_estado)` sin recargar. Rebuild 92 MB + E2E verificado (arranque sin sesión → Guardar → Conectar inválido 400 sin "conectado" falso → Detener conserva sesión). Pendiente: push + prueba con token real + diagnóstico del "/buscar se queda en Buscando...".

## Estado actual (previo)
Pack 12/sep/2026 tarde (commiteado en `637da6b`): plan de 7 fixes tras reportes del usuario ("No se pudo iniciar el bot. Revisa token e ID.", skeleton no visible, solo Ctrl+V pega).

Diagnóstico: `dist\ytremote\ytremote.exe` directo NO arrancaba el bot porque faltaban `runtime\`, `src\` y `config.json` junto al exe; y en el ZIP, **causa raíz del "conectado" falso**: `release_rules.json` no incluía `security.py` en la allowlist de `src/` → `config.py` moría con `ModuleNotFoundError`; y el ZIP iba sin `data/` → `sqlite3.OperationalError: unable to open database file` al crear `roles.db`. Ambas mataban el bot a los ~2s, después del chequeo de 0.6s.

Fix aplicado: (1) `bot_process.py` ahora valida existencia de python/runtime, escribe `data/bot.log` (stdout+stderr del subproceso vía hilo vigía) y expone `last_error()`; (2) token se valida con **getMe de Telegram ANTES** de guardar sesión/arrancar → respuesta honesta "Token inválido: {motivo}" (nunca más "conectado" falso); (3) `release_rules.json` suma `security.py`; (4) `roles.py` crea `data/` (mkdir); (5) `release_zip.py` SIEMPRE embebe `data/`; (6) `build.py --zip` ahora **materializa el dist**: extrae el zip EN `dist\ytremote` → dist queda portable completo (runtime+src+config junto al exe); (7) skeleton servido como `data:` URL base64 con logo embebido (el `file://` se rompía en WebView2); (8) menú contextual Pegar/Copiar propio (WebView2 no tiene menú nativo) + limpieza de caracteres invisibles (zero-width) que invalidaban el token al pegar con Ctrl+V.

Verificado: POST /api/connect con token inválido → 400 `Token inválido: Unauthorized` y NO guarda sesión; bot arrancado directo del bundle → proceso vivo + `data/roles.db` creado; exe real sirve `/launcher` (200), `/static/skeleton.html` (200), `/api/session` (200). Falta la prueba con token real (depende del usuario).

Los fixes del pack anterior (12/sep mañana: skeleton instantáneo, `/buscar` diagnóstico, `settings.enc`) quedaron en `b605228` + `f9a3e0e`, aún sin push.

El crash "Main window failed to start" sigue resuelto (la ventana ya no llama `show()`/`hide()` prematuramente; auto-conexión en `webview.start(func)`).

UI aprobada y aplicada: paleta Telegram (azul #229ed9 primario, fantasma secundario, rojo #e53935 destructivo, verde #31c471 conectado; nada de dorado), botones en línea (Conectar+Salir y Detener+Cerrar sesión en fila), logo `logo-ytremote.png` en el encabezado del formulario e icono del exe desde `ytremote.ico` en ambas builds (`build.py` y `build_exe.py`).

Ventana subida a 420x640 y centrada en el **área de trabajo** (SPI_GETWORKAREA: pantalla menos barra de inicio) horizontal y verticalmente, con corrección DPI. Formulario compactado y CSS anti-scroll (`overflow:hidden` en html/body + `#form-container` con scroll interno): el logo nunca se corta ni aparece barra de scroll.

Commiteado y pusheado a `origin/main` como v1.0.0 (`6b67efc`): `README.md` y `GUIA.txt` actualizados al flujo del exe (ventana del launcher, `session.enc`, tray, WebView2). `.gitignore` ajustado (se versiona `static/`; se ignoran `dist`/`build`/`node_modules`/`session.enc` y volcados de prueba).

Pipeline de release v1.0.0 EN MARCHA: `release_zip.py` extrae la lógica del ZIP a un módulo compartido (`build.py --zip` y `build_exe.py` usan la misma fuente). `build.py --zip` ahora arma un ZIP LEAN: `ytremote.exe` + `_internal` a la raíz, y junto a ellos `runtime/` (site-packages solo con la allowlist), `src/` del bot (10 módulos), `config.json`, `README.md` y `GUIA.txt`. Allowlist validada contra imports reales (se recortaron `colorama`, `iniconfig`, `packaging`; se conservaron `apscheduler`/`tzdata`/`tzlocal` porque `bot.py` usa `job_queue.run_repeating`). `data/roles.db` dejó de versionarse (trae IDs reales de Telegram).

**✅ RELEASE v1.0.0 PUBLICADO** (11/sep/2026): `yt-remote-1.0.0-portable.zip` = 92.0 MB (3.461 entradas; los viejos 0.5.0 pesaban hasta 132 MB con pip/pytest adentro). Build `python build.py --zip` (commit `9182276`, push `6b67efc..9182276`). Release: https://github.com/drfoxsoscomputer/yt-remote/releases/tag/v1.0.0

## Pendientes / ToDo
- [ ] Push de packs (12/sep) a origin/main — pendiente confirmación explícita
- [ ] Emparejar con token REAL desde el exe/dist nuevo: Conectar → getMe aprueba → bot spawn desde runtime portable → `data/bot.log` sin errores → responder en Telegram + tray
- [ ] "Se queda en Buscando...": bug del bot `/buscar` (no del launcher). Diagnóstico: `search()` standalone responde bien (3 resultados, 7.5s dev / 4.4s runtime). Sospecha en `bot.py _run_search` (~1796-1906): flujo de edición del mensaje "🔎 Buscando..." (fallback send_message si edit falla). Revisar con `data/bot.log` + token real
- [ ] Inicio con Windows (auto-arranque al encender la PC) — anotado por el usuario para EL FINAL, cuando todo funcione
- [ ] Pull/verificar en máquina limpia: skeleton visible, launcher, `/buscar` diagnóstico, arranque sin `.env`
- [x] **E2E v1.1.1 verificado por el usuario (14/sep)**: revisó el zip/exe publicado en el flujo real → conexión directa con la sesión guardada, minimización al tray en En línea, avatar-dot pegado (4 px, color por estado), colores por estado (verde/rojo/gris) y Power nítido. Todo bien. Los E2E intermedios de builds anteriores (colores, 7 fixes, fixes 1-2, flatMap, arranque animado, polish, pack 1.1.0) quedan absorbidos por esta verificación final sobre el release.
- [x] **E2E visual v1.1.2 por el usuario (14/sep, dev server)**: probado el dot en 127.0.0.1:8081/launcher antes del release — "quedó justo como quería" (mitad adentro/mitad afuera del círculo del avatar). Sin tests nuevos tras el release (el cambio es cosmético único, el resto del bundle es de v1.1.1 ya verificado).

## Decisiones recientes
- 14/sep/2026: **FIDELIDAD DE PORTABILIDAD (WebView2 del sistema)**: el zip es portátil en el stack propio (Python/mpv/bot viajan completos en la carpeta), pero la ventana usa el componente WebView2 del sistema (ya preinstalado en Windows 11 y en Windows 10 actualizados; si falta, el exe muestra el cartel de instalación en `main_launcher.py:437`). Se eliminó el claim literalmente falso ("portable de verdad", "no necesita nada externo") de README y se redactó la verdad exacta en README/GUIA y en las notas del release. Opción A elegida por el usuario (mantener WebView2 + wording honesto); la portabilidad literal 100% (empaquetar CEF) queda como trabajo futuro si algún día se pide. Rebuild del zip 1.1.1 para que sus docs embebidas lleven el wording corregido.
- 14/sep/2026: **RELEASE 1.1.2 por patch visual**: el badge del avatar quedaba separado del círculo del avatar; ahora `-bottom-0.5 -right-0.5` (2 px, mitad adentro/mitad afuera del círculo). Se aplicó en `templates/launcher.html:52` y `static/js/launcher.js:234` (setClases re-aplica la posición en cada pintado). Se recompiló `tailwind.css` para incluir las nuevas clases (el CSS compilado estaba STALE y no las contenía). Fix cosmético único → bump de parche (semver).
- 14/sep/2026: **RELEASE 1.1.1 por convención semver**: los cambios del 14/sep (pintado de colores, avatar-dot, rutina post-build/conexión) son fixes → bump de parche. v1.0.0→1.1.0 había sido característica. Fuente de versión ÚNICA: `release_rules.json` (`version`), nombre del zip = `yt-remote-{version}-portable.zip` (sin prefijo `v`). Build: `python build.py --zip`.
- 14/sep/2026: **RUTINA POST-BUILD obligatoria tras CADA `python build.py`**: 1) `C:\Users\DrFox\AppData\Local\Temp\opencode\extraer_portable.ps1` (restaura `runtime/`+`src/`+`config.json` desde el zip existente, nunca exe/_internal), 2) restaurar `data/` del snapshot. El 2º rebuild del día falló en esto y el usuario volvió a ver "no conecta" (runtime ausente junto al exe). Además, RESTAURAR `data/` SOLO en directorio EXISTENTE o archivo por archivo (nunca `Copy-Item snap\* destinoInexistente` → crea un archivo basura que rompe `session.enc` → `has_session:false`).
- 14/sep/2026: Avatar-dot iteraciones: `-bottom-3`/-`right-3` (12 px, demasiado lejos) → `-bottom-1`/`-right-1` (4 px, tangente al círculo visible) → `-bottom-1.5`/`-right-1.5` (6 px, cabalgando la esquina del cuadrado pero tangente al círculo visible) → FINAL `-bottom-0.5`/`-right-0.5` (2 px, mitad adentro/mitad afuera, aprobado visualmente por el usuario en dev server).
- 13/sep/2026: **Icono Power único en todos los estados**: el usuario trajo `assets/power-button.svg` (power ON/OFF clásico: aro + vástago) porque el `ICONO_SPINNER` que rotaba en CONECTANDO "no se parecía en nada" a un power. Se inyectan las 2 rutas visibles (clase `.st0`: aro abierto + vástago; `.st1` aro exterior y `.st2` disco blanco tienen `display:none`) con `fill="currentColor"` → mismo icono teñido por estado (verde/rojo/gris). Nunca más intercambio de innerHTML ni `animate-spin`. El SVG fuente se sirve en `/static/img/power-button.svg` (dev y bundle).
- 13/sep/2026: **Icono Power tamaño doble (128×128 px)**: a petición del usuario, `#power-icon` pasó de `w-16 h-16` (64px) a `w-32 h-32` (128px) en `templates/launcher.html:88`. Botón se mantiene en 144px (`w-36 h-36`). Se sincronizó el HTML y JS a la versión nueva del SVG (viewBox 800, 2 rutas sin disco blanco). Probe en vivo OK: ciclo boot → En línea → 3 s → minimiza al tray.
- 13/sep/2026: **Telemetría JS→bot.log para depura el launcher en vivo**: `POST /api/log` + `uiLog()` en launcher.js + `req()` con AbortController (timeouts: 15 s general, 20 s /api/start, 10 s /api/status) + captores globales. Gracias a esto se detectó en el PRIMER probe la causa raíz `lista.flatMap is not a function` que congelaba la UI. `tokens()` NO debe usar `flatMap`/`.at()`: recibe strings y `String.prototype.flatMap` no existe.
- 13/sep/2026: Spec de la ventana (literal del usuario): "la ventana queda visible mientras conecta y, cuando el bot queda En línea, espera unos 3 segundo y luego se va sola al tray" → implementado en `esperarYMinimizar()` (`TRAY_TRAS_EN_LINEA=3000`) en el boot y tras el clic de Conectar.
- 13/sep/2026: El ZIP/versión solo se toca cuando el usuario pide el release. PARA PROBAR SIN ZIP: `python build.py` (a secas) recompila solo el exe; luego restaurar la portabilidad con `C:\Users\DrFox\AppData\Local\Temp\opencode\extraer_portable.ps1` (extrae `runtime/`, `src/`, `config.json`, docs del zip 1.1.0 existente — nunca exe/_internal); `data/` se respalda (snapshot `ytremote-data-snap-20260913`) antes del build y se restaura después. El probe final: exe con CWD=dist\ytremote + lectura de `data/bot.log` (líneas `[UI]`) + kill de procesos (el "1 restante" era el propio pwsh del comando = falso positivo).
- 13/sep/2026: Arranque con sesión = la página manda: Python arranca el bot pero NO esconde la ventana; la UI pinta "Conectando…" verde neón (mínimo `MIN_CONEXION=3000`ms) y minimiza al tray vía `pywebview.api` recién con `bot_running=true`. Si no enciende, la ventana se queda mostrando el estado real (evita el parpadeo tray→show→tray).
- 13/sep/2026: Colores ESTADO reales por principio: `pintarEstado()` borra SIEMPRE primero (listas `CLS_CHIP`/`CLS_DOT` en el 3er arg de `setClases`) todas las clases de color viejas antes de pintar las nuevas; el chip nunca más mezcla rojo+verde.
- 12/sep/2026 (noche): Lote "cierre limpio + todo neutro Venezuela" (5 tareas): X de ventana → tray directo SIN ningún diálogo (`_on_closing` ya no pregunta; salir = tray "Salir" o Cerrar sesión); ✕ interna del header eliminada; `cargarEstado()` llama `mostrarVista('estado')` (arregla Atrás y el retorno tras Guardar); Guardar = SVG floppy + texto (usuario: emojis "feos, infantiles y nada profesionales"); Spanish: TODO en usted sin voseo, también en bot.py (solo 3 strings reales tenían voseo) y setup_cli.py.
- 12/sep/2026 (noche tardía): Rediseño State-Driven del launcher (spec del usuario, ayuda visual tipo ProtonVPN/Docker): botón Power central verde/rojo/gris/spinner unifica Estado+Acción; acciones secundarias (Config/Cerrar sesión) al dropdown del ⚙️; ✕ = minimizar al tray sin diálogo; chip estado + @handle real + link t.me; footer de estado. Paleta verde #22c55e / rojo #ef4444. Una sola fuente de verdad (estados explícitos, nunca deducidos de botones). `bot_username` persistido en la sesión.
- 12/sep/2026 (noche): Launcher de DOS PANTALLAS (flujo definido por el usuario): con datos al arrancar → auto-conecta → tray; sin datos → "Debes iniciar sesión" → Iniciar sesión (formulario) → Guardar (solo guarda, NO arranca) → vuelve a principal → Conectar → conecta → tray. Detener no navega ni borra; Cerrar sesión es lo único que borra (con confirmación); Configurar precarga. `POST /api/save` (guardar sin validar getMe) y `POST /api/start` (usa sesión guardada, getMe antes de arrancar). Encriptación DPAPI se MANTIENE (nunca fue la causa; quitarla deja token en texto plano; no mover a roles.db). commit `637da6b`
- 12/sep/2026 (tarde): "Conectado" honesto => `bot_process.validate_token()` hace getMe contra Telegram ANTES de guardar `session.enc`/arrancar el bot, y launcher responde el motivo real de `last_error()`. El "conectado" falso del usuario era doble caída: (a) ZIP sin `security.py` (allowlist `release_rules.json`) → `ModuleNotFoundError` en `config.py`; (b) ZIP sin carpeta `data/` → `sqlite3.OperationalError` al crear `roles.db` en `RoleManager._load`. Fixes: allowlist suma `security.py`; `release_zip.py` embebe SIEMPRE `data/`; `roles.py` y `security.save_bot_data` crean `data/` con mkdir.
- 12/sep/2026 (tarde): `build.py --zip` ahora materializa el dist: `materialize_portable_dist()` extrae el zip EN `dist\ytremote`; así `dist\ytremote` nunca más queda sin `runtime\python\python.exe` (causa del "No se pudo iniciar el bot" original al correr el exe a secas). Además `bot_process.start()` valida existencia de python + main.py con errores concretos y vuelca la salida del subproceso a `data/bot.log` (hilo vigía con timeout 30s) — con `--windowed` eso antes era invisible.
- 12/sep/2026 (tarde): Skeleton vía `data:` URL (base64) en vez de `file://` — WebView2 no renderizaba fiable el `file://` y rompía las rutas relativas; `_data_uri_skeleton()` embebe el HTML + logo (`img/logo-ytremote.png` → base64) con fallback al `http://127.0.0.1:8081/static/skeleton.html`.
- 12/sep/2026 (tarde): Menú contextual propio en el launcher (WebView2 no crea menú nativo: solo Ctrl+V). `LauncherApi.pegar()` lee el portapapeles vía ctypes (OpenClipboard/CF_UNICODETEXT) y el JS limpia caracteres cero-ancho (zero-width) que invalidaban el token al pegar.
- 12/sep/2026: Launcher con skeleton instantáneo (commit `b605228`): la ventana nace de inmediato con `static/skeleton.html` (file://, CSS inline + shimmer, paleta #1a1a2e) en vez de esperar que Flask arranque. `main_launcher.py` reescrito: `_skeleton_url()`, `_cargar_interfaz_real()` (espera Flask hasta 10s en hilo daemon → `load_url('/launcher')` → auto-conexión si hay sesión → minimiza a tray). Flask arranca con `webview.start(_post_start)`.
- 12/sep/2026: `/buscar` con diagnóstico real (raíz: yt-dlp con client web era bloqueado INTERMITENTE por "Sign in to confirm you're not a bot"; el error se tragaba en `search()`). Fix: `search.py` prueba primero `player_client visionos` (el mismo que esquiva el bloqueo en `resolve_stream_url`) con fallback al default, y expone la causa en `last_search_error()`; `bot.py` añade `_SEARCH_TIMEOUT = 30.0` (asyncio.wait_for) y responde el motivo real por Telegram en vez del genérico "No encontre resultados."
- 12/sep/2026: Fin del `.env` para ALLOWED_CHAT_ID (lo pedía el usuario; era texto plano). Ahora `data/settings.enc` con DPAPI (mismo mecanismo que session.enc): `security.py` gana `load/save_bot_data`, `get/set_allowed_chat_id`, `migrate_dotenv_allowed_chat()` (mueve el valor viejo al arranque y borra el `.env` si queda vacío; conserva TELEGRAM_TOKEN/OWNER_ID si el flujo manual legacy los usa). `config.py` resuelve env → settings.enc → `.env` legacy; `bot.py` `/start` escribe encriptado; `setup_cli.py` delega al store. `.env` ya no se crea en el flujo del launcher.
- 12/sep/2026: `launcher_web.py` servía `/launcher` y `/static/` solo empaquetado (Flask resolvía carpetas relativas a `src/` en dev → 404/500). Fix: `RES_DIR` absoluto (módulo `_internal` si frozen, raíz del repo en dev) para `template_folder`/`static_folder`; dev y bundle sirven idéntico.

## Decisiones recientes
- 11/sep/2026: Crash "Main window failed to start" = `show()` prematuro. Fix: mover auto-conexión a `webview.start(func)` y quitar `show()` del flujo sin sesión en `src/main_launcher.py`. Sin sesión la ventana nace visible (`hidden=False`); con sesión el bot arranca post-GUI y la ventana se oculta al tray.
- 11/sep/2026: Build portable: runtimes WebView2 vendedados en `assets/webview2/runtimes/`; `build.py` apunta ahí (antes hardcodeaba `D:\laragon\...\webview\lib\runtimes`, lo que rompía el exe sin WebView2Loader.dll fuera de esa máquina).
- 11/sep/2026: Eliminada la doble auto-conexión: Python arranca el bot (sesión), `launcher.js` solo refleja `/api/status` (antes hacía POST /api/connect con token truncado).
- 11/sep/2026: Paleta Telegram en el launcher. Se eliminó el acento dorado (--oro) que molestaba al usuario; Conectar pasó de rojo YouTube a azul Telegram #229ed9 y Salir/Detener a botón fantasma (para no quedar iguales en fila). Logo `assets/logo-ytremote.png` copiado a `static/img/` (Flask sirve `static/` al lado del exe; la ruta `assets` no se sirve). `build_exe.py` ahora asigna `ytremote.ico` al exe onefile (antes `icon=''`).
- 11/sep/2026: Logo se cortaba por overflow: ventana 420x520 no alcanzaba (~636px de contenido). Fix: ventana → 420x640, centrado contra el área de trabajo con `SystemParametersInfoW(SPI_GETWORKAREA)` (antes `GetSystemMetrics` de pantalla completa, que incluía la barra de inicio), y CSS anti-scroll (`html,body{overflow:hidden}` + `#form-container` scrollea interno). Medido en 100% DPI: T=96, B=736, barra en 834.
- 11/sep/2026: UI aprobada por el usuario y primer release del launcher (v1.0.0). Documentación actualizada al flujo real del exe: `README.md` y `GUIA.txt` descartan el wizard `ytremote.bat`/`.env` y explican la ventana del launcher (token + ID admin + horas kick, botones Conectar/Salir/Detener/Cerrar sesión, `session.enc`, tray, WebView2). Convención: artefactos escritos en español neutro (tono Venezuela).
- 11/sep/2026: Pipeline de ZIP portable unificado en `release_zip.py` (release_rules.json = única fuente para `build.py --zip` y `build_exe.py`). `build.py --zip` arma ZIP lean (exe + `_internal` + runtime allowlist + `src` del bot + `config.json` + docs). Allowlist recortada (fuera `colorama`/`iniconfig`/`packaging`; dentro `apscheduler`/`tzdata`/`tzlocal` por `job_queue`). `data/roles.db` (IDs reales de Telegram) sale del versionado.
- 11/sep/2026: Logo `static/img/logo-ytremote.png` agregado centrado al inicio del README (el pedido del usuario quedó pendiente en la tanda anterior por mi error al archivarlo como "pendiente" en vez de ejecutarlo; se corrigió en esta edición).

## Tests / verificación
- ✅ 14/sep (release v1.1.2 FULL PIPELINE 7/7): dev server detenido → `release_rules.json` bump a `"1.1.2"` → `git push origin main` (commits `b654638` docs E2E + `94433a5` fix badge) → `python build.py --zip` → exe 18.1 MB + `yt-remote-1.1.2-portable.zip` 92.0 MB (3479 entradas) → `data/` restaurada del snapshot (3 MD5 idénticos: session.enc 1DF710C3, bot.log ABAF3449, roles.db BEA63939) → zip verificado: dot `-bottom-0.5` en `_internal/templates/launcher.html`, `_internal/static/js/launcher.js`, `_internal/static/css/tailwind.css` (clase `. -bottom-0\\.5{bottom:-.125rem}` presente); `data/` vacía (sin datos sensibles) → commit `9fc54f4` (release 1.1.2) + push → `gh release create v1.1.2` con `--latest` y notas ES neutro (https://github.com/drfoxsoscomputer/yt-remote/releases/tag/v1.1.2); release verificado: `latest=true`, tag `v1.1.2`, no-draft, no-prerelease → MD5 fuente↔empaquetado (normalización LF): `launcher.js` 6b82ede…, `launcher.html` c376896…, `tailwind.css` 2179049… todos OK dist↔zip↔fuente
- ✅ 14/sep (rebuild docs v1.1.2 re-subido al release): tras commit de docs `4225146` (ESTADO v1.1.2 + README "Versión actual: v1.1.2"), `python build.py --zip` rebuild (el README embebido en el zip pasó de v1.1.1 a v1.1.2) → `data/` restaurada (3 MD5 idénticos) → `gh release upload v1.1.2 --clobber` re-subió el zip actualizado (96,485,988 B). Verificado en el zip: README embebido dice v1.1.2, dot `-bottom-0.5` sigue en html/js/css, `data/` vacía. Release intacto (publishedAt sin cambios). Push del commit de docs `4225146` confirmado (9fc54f4..4225146).
- ✅ 14/sep (E2E VISUAL del usuario, v1.1.1): el usuario revisó el zip/exe publicado (`yt-remote-1.1.1-portable.zip`) en el flujo real y confirmó que quedó bien: conexión directa con su sesión guardada, minimización al tray al quedar En línea, avatar-dot pegado (4 px, color por estado) y colores de estado correctos (verde/rojo/gris)
- ✅ 14/sep (rebuild docs v1.1.1): `python build.py --zip` → exe 18.1 MB + `yt-remote-1.1.1-portable.zip` (92.0 MB, 3479 entradas); zip con `README.md`/`GUIA.txt` con el wording nuevo de WebView2 del sistema; SIN `session.enc`/`roles.db`/`bot.log` (no filtra datos); `_internal/static/js/launcher.js` MD5 ≡ árbol (8A5DF18); `dist\ytremote` materializado portable + `data/` real restaurada del snapshot `20260914c` (3 MD5 idénticos)
- ✅ 14/sep (release zip v1.1.1): `python build.py --zip` → `yt-remote-1.1.1-portable.zip` (92.0 MB, 3479 entradas); zip abierto y verificado: rutas con `/` (no `\`) en FullName; presentes exe/runtime/python/src/main/config.json/README/GUIA/data/ + `_internal` JS/HTML; `launcher.js` del zip MD5 ≡ árbol (8A5DF18, = fix E4B42C con CRLF por autocrlf) y HTML con dot pegado; zip SIN `session.enc`/`roles.db`/`bot.log` (no filtra datos); `dist\ytremote` materializado portable + `data/` real restaurada (3 MD5 idénticos al snapshot c); bundle `launcher.js` ≡ árbol
- ✅ 14/sep (avatar-dot pegado + conexión + rutina post-build): `node --check` OK; simulación node del dot: las 5 estados con `-bottom-1 -right-1` + color correcto (`bg-verde/rojo/texto-dim`); rebuild 18.1 MB; `extraer_portable.ps1` (2243 archivos) + `data/` restaurada; MD5 post-fix `launcher.js`/`launcher.html` idénticos árbol↔`_internal` (E4B42CBF..., DBA02988...) y CSS sin cambios (7935BB08..., 3C113CA6...); smoke exe real: `/launcher` 200, `/api/session` `has_session:true`, `/api/start` 200 `ok:true` "Bot conectado", `/api/status` `bot_running:true`, JS servido con `-bottom-1 -right-1`; `data/` MD5 idénticos al snapshot `20260914c`, procesos 0, socket cerrado. GOTCHA detectado en el camino: `Copy-Item "$snap\*"` con destino inexistente crea un archivo `data` de 390 B en vez de los 3 archivos → data/session.enc faltante → `has_session:false` (reparado recreando `data/` + copia por archivo)
- ✅ 14/sep (fix colores de estado — setClases add/remove): simulación node del flujo real (clases estáticas del template + remove→add) → los 5 estados quedan con TODAS sus clases de color (sin-sesion gris/border-borde/power-neutral; conectando+encendido verde + pulse + group-hover:text-verde-hover; apagado+error rojo + group-hover:text-rojo-hover + power-idle/power-error); `node --check` OK; rebuild exe 18.1 MB; MD5 `launcher.js` árbol↔`_internal` idéntico (146B4235...); bundle con orden remove→add (l.41-42); smoke exe real: `/launcher` 200, `/static/js/launcher.js` servido con MD5 idéntico y orden remove→add, `/api/session` 200 `has_session:true` + no-store; `data/` restaurada, procesos 0, socket 8081 cerrado
- ✅ 14/sep (rebuild fixes 1-7 + smoke exe real): `python build.py` (18.1 MB; 1er intento falló: bot del smoke previo tenía `runtime\python\libcrypto-3.dll` → matado PID 2744) → `_internal` con w-20/h-20, guarda `sin minimizar`, `location.reload` único en rama else, tema.css sin form-container, MD5 ×3 idénticos árbol↔`_internal`. Exe real CWD=dist\ytremote: 200 ×5; no-store; evil Origin → 403; legit Origin kick 999 → sesión kick 168; sin Origin → 200. `data/session.enc` restaurada del snapshot (MD5 idéntico), procesos 0.
- ✅ 14/sep (fixes 3-7 auditoría, dev): server `python -m src.launcher_web` → `/api/session` 200 `Cache-Control: no-store`; `/api/start` sin sesión → 400 `"Inicie sesión primero."`; POST `Origin: http://evil.example` → 403 `Origen no permitido`; POST sin Origin → 200; POST `Origin: http://127.0.0.1:8081` → 200; `kick_after_hours:999` → guarda 168 en sesión. `py_compile` ×3 y `node --check` OK; `data/session.enc` dev respaldado/restaurado; server bajo, sin procesos residuales.
- ✅ 14/sep (rebuild tras fixes 1-2): `python build.py` → exe 18.1 MB; `_internal` con `w-20`/`h-20` y JS guarda `sin minimizar` (l.520); MD5 `launcher.js`/`tailwind.css` idénticos árbol↔`_internal`; smoke exe real (8081 libre) → 200 ×5 (`/launcher`, `/api/session`, `/api/status`, `/static/js/launcher.js`, `/static/css/tailwind.css`); servido con guarda ENCENDIDO + `.w-20`/`.h-20`; sin procesos residuales al matar
- ✅ 13/sep 01:06 (causa raíz real del "Sin sesión" persistente): exe reconstruido (`python build.py` sin zip, exe 19 021 022 B) lanzado con **CWD = `dist\ytremote`** (escenario exacto del usuario, antes no probado) → `/static/js/launcher.js` 200 (21840 B, igual al árbol), `/static/css/tailwind.css` 200, `/static/css/tema.css` 200, `/static/img/logo-ytremote.png` 200, `/static/skeleton.html` 200, `/launcher` 200 con `glyph power` y sin `reloj`; `/api/session has_session:true` (`bot_username` vacío porque la sesión 358 B es la que guardó el usuario a las 00:46), `/api/status bot_running:true`; JS servido con `MIN_CONEXION` y `powerIcon.innerHTML`. Procesos 0 y `data/` intacta tras correr+matar. Antes de esté fix, con este mismo CWD los estáticos daban 404 y `launcher.js` nunca cargaba.
- ✅ 13/sep 00:09: build de PRUEBA sin zip (`python build.py` a secas) → `dist\ytremote\ytremote.exe` 18.1 MB con el lote de arranque verde + colores reales + sin "Abrir en Telegram". Portabilidad restaurada desde el zip 1.1.0 existente (solo `runtime/`, `src/`, `config.json`, docs — 2243 archivos, SIN tocar exe/_internal/templates/static/versión) → dist queda usable y el zip viejo intacto. `data/` respaldado a temp antes del build (PyInstaller borra dist) y restaurado después (session.enc de pruebas conservada). Verificado en `_internal`: `minimizar_tray`, `CLS_DOT`, `MIN_CONEXION=3000`, sin `linkTelegram`, sin `link-telegram`/`Abrir en Telegram`, `poderNeon` presente, `poderLatido` ausente. Pendiente: E2E visual del usuario con el exe.
- ✅ 13/sep (fix UI muerta): harness Node DOM-stub sobre `static/js/launcher.js` → `HARNESS_OK refresh_estado=function` + listeners `["window:error","window:unhandledrejection","contextmenu","mousedown","DOMContentLoaded"]` + 9 listeners de elementos (powerBtn/menuBtn/menuConfig/menuLogout/btnSave/btnBack/toggleToken/ctx copiar/pegar). 27/27 ids del JS existen en el HTML; `node --check` OK; exe de prueba (18.1 MB): `/api/session` `has_session:true`, `/api/status` `bot_running:true`, JS servido 21148 B/582 líneas idéntico al árbol, CSS `poderNeon`, HTML sin `link-telegram`, `data/` intacta tras correr+matar (bot.log regenerado, roles.db actualizado)
- ✅ 12/sep noche (E2E exe `dist\ytremote`): arranque sin sesión → `/api/session` `{"has_session":false}`, `/launcher`/`/static/js/launcher.js`/`/static/css/tailwind.css` 200; POST `/api/save` (token formato válido falso + admin) → 200 y `/api/session` devuelve datos completos (precarga formulario); POST `/api/start` → 400 `Token inválido: Unauthorized` con `bot_running:false` (sin "conectado" falso); POST `/api/stop_bot` → 200 y sesión CONSERVADA (Detener no borra)
- ✅ 12/sep noche: endpoints nuevos en dev (Flask): save guarda sin validar, start rechaza token inválido; `session.enc` dev borrado tras prueba
- ✅ 12/sep noche: `py_compile` launcher_web + main_launcher OK sin warnings; `node --check static/js/launcher.js` OK
- ✅ 12/sep tarde: POST `/api/connect` con token inválido → 400 `Token inválido: Unauthorized`, NO guarda sesión (antes decía "conectado")
- ✅ 12/sep tarde: bot arrancado directo del bundle (`dist\runtime\python\python.exe src\main.py`): con `security.py` y `data/` corregidos → proceso vivo a los 10s + `data/roles.db` creado
- ✅ 12/sep tarde: exe real sirve `/launcher` (200, 6544B), `/static/skeleton.html` (200, 2740B), `/api/session` (200); `dist\ytremote` portable completo (runtime/src/config.json presentes)
- ✅ Rebuild `python build.py` (PyInstaller 6.22, Python 3.13) → `dist\ytremote\ytremote.exe` con `assets/webview2/runtimes` en el bundle (win-x64/x86/arm64)
- ✅ Sin sesión (camino que crasheaba): exe levanta Flask, proceso vivo >25s, `/api/status` `{"bot_running":false}`, formulario servido — sin crash
- ✅ Con sesión: exe detecta `session.enc`, spawn del bot (`runtime\python python.exe src\main.py`), `bot_running=true`, ventana oculta + tray, exe estable — sin crash
- ✅ Errores de arranque ahora saltan MessageBox (antes: `--windowed` sin consola = errores invisibles)
- ✅ `runtime\python` (portable) + `src\main.py` + `config.json` copiados al lado del exe durante la prueba y retirados al final (dist quedó limpio)
- ✅ UI: `/launcher`, `/static/img/logo-ytremote.png` (PNG válido), `/static/css/tailwind.css` y `/static/js/launcher.js` = 200 en el exe nuevo; CSS servido sin `oro`, con azul #229ed9, `flex-1` y `gap-3`; HTML sin `titulo-oro`/`ring-oro` y con `<img>` del logo
- ✅ Bundle: `_internal\static\img\logo-ytremote.png`, `templates`, `ytremote.ico`, runtimes win-x64/x86/arm64 presentes
- ✅ Aprobado visualmente por el usuario (11/sep/2026). UI aceptada tal como quedó
- ✅ Tamaño/centrado: ventana 420x640, L=558 (centro horizontal de 1536), T=96 (centro vertical del área de trabajo 834), B=736 < 834 = no pisa la barra de inicio; proceso estable, HTTP 200
- ✅ Logo completo + sin scroll: contenido ~600px < ventana 640px (antes ~636px en 520px = overflow/corte); red de seguridad CSS activa en el bundle